import { create } from "@stylexjs/stylex";

const ratingsWrapperStyles = create({
  wrapper: {
    display: "flex",
    justifyContent: "center",
    width: "100%",
    paddingTop: 15,
  },
});

export { ratingsWrapperStyles as styles };
